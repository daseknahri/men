# ─── App ───────────────────────────────────────────
# The domain name Traefik will route to this app (VPS only)
DOMAIN=foody.yourdomain.com

# Host port to expose locally (dev only, ignored when using Traefik)
HOST_PORT=8090

# Timezone
TZ=Africa/Casablanca
