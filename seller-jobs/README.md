# Seller Jobs

This folder is the seller-only workspace for staged import and bundle-generation jobs.

Runtime job runs are stored under `seller-jobs/runs/` and are intentionally ignored from git.

Use this workspace for:

- uploaded source-file copies
- raw extraction outputs
- normalized bundle drafts
- review summaries
- apply-ready artifacts
